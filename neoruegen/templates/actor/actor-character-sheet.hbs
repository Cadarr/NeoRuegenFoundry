<form class="{{cssClass}} {{actor.type}} flexcol" autocomplete="off">
  <header class="sheet-header">
    <img class="profile-img" src="{{actor.img}}" data-edit="img" title="{{actor.name}}" height="100" width="100"/>
    <div class="header-fields">
      <h1 class="charname">
        <input name="name" type="text" value="{{actor.name}}" placeholder="{{localize 'NEORUEGEN.Sheet.Character'}}"/>
      </h1>
      <div class="resources grid grid-3col">
        {{#each system.attributes as |attribute key|}}
        <div class="resource flex-group-center">
          <label for="system.attributes.{{key}}.value" class="resource-label">{{attribute.label}}</label>
          <div class="resource-content flexrow flex-center flex-between">
            <input type="number" name="system.attributes.{{key}}.value" value="{{attribute.value}}" min="1" max="6" step="1" data-dtype="Number"/>
          </div>
        </div>
        {{/each}}
      </div>
    </div>
  </header>

  <nav class="sheet-tabs tabs" data-group="primary">
    <a class="item" data-tab="character">{{localize "NEORUEGEN.Sheet.Character"}}</a>
    <a class="item" data-tab="biography">{{localize "NEORUEGEN.Sheet.Biography"}}</a>
  </nav>

  <section class="sheet-body">
    <div class="tab character" data-group="primary" data-tab="character">
      <section class="grid grid-3col">
        <aside class="sidebar">
          <h2>{{localize "NEORUEGEN.Sheet.Attributes"}}</h2>
          <div class="attributes flexcol">
            {{#each system.attributes as |attribute key|}}
            <div class="attribute flexrow flex-group-center">
              <label for="system.attributes.{{key}}.value" class="resource-label flexlarge align-left">{{attribute.label}}</label>
              <input type="number" name="system.attributes.{{key}}.value" value="{{attribute.value}}" min="1" max="6" step="1" data-dtype="Number"/>
            </div>
            {{/each}}
          </div>
        </aside>

        <section class="main grid-span-2">
          <h2>{{localize "NEORUEGEN.Sheet.Skills"}}</h2>
          <div class="skills grid grid-2col">
            {{#each system.skills as |skill key|}}
            <div class="skill flexrow flex-group-center">
              <label for="system.skills.{{key}}.value" class="resource-label flexlarge align-left">{{skill.label}}</label>
              <input type="number" name="system.skills.{{key}}.value" value="{{skill.value}}" min="0" max="4" step="1" data-dtype="Number"/>
            </div>
            {{/each}}
          </div>
        </section>
      </section>
    </div>

    <div class="tab biography" data-group="primary" data-tab="biography">
      {{editor enrichedBiography target="system.biography" engine="prosemirror" button=true editable=editable}}
    </div>
  </section>
</form>
